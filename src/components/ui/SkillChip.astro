---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="inline-flex items-center rounded-full border border-border bg-white/[0.03] px-3 py-1 font-mono text-xs text-text-muted transition-colors hover:border-border-accent hover:text-accent">
  {label}
</span>
